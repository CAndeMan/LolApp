@{
    ViewBag.Title = "Champions";
}

@Model ChampionListModel
<h2>@ViewBag.Title.</h2>
<h3>@ViewBag.Message</h3>


@foreach (var item in Model.list)
{
    <div>
        <h4>@item.id</h4>
        <p>@item.freeToPlay</p>
    </div>
}

<p>Use this area to provide additional information.</p>
